get:
  tags:
    - server
  summary: List of databases
  description: Returns all the databases on the running instance.
  responses:
    200:
      description: List of the names of the databases on the server.
      schema:
        type: array
        items:
          type: string
          description: Database name