<?xml version="1.0" encoding="UTF-8"?>
<article xmlns:xi="http://www.w3.org/2001/XInclude"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="../../../docs.xsd"
         id="limitations">
   <title>Limitations</title>
   <description>TODO: Add description.</description>
   <introduction>
      <paragraph>Sync Gateway has the following limitations:</paragraph>
      <unordered-list>
         <list-item>
            <paragraph>It cannot operate on pre-existing Couchbase buckets with app data in them because Sync Gateway has its own document schema and needs to create and manage documents itself. You can migrate existing data by creating a new bucket for the gateway and then using the Sync REST API to move your documents into it via PUT requests. You can’t make changes to the Couchbase bucket directly. You have to go through the Sync Gateway API.</paragraph>
         </list-item>
         <list-item>
            <paragraph>Explicit garbage collection is required to free up space, via a REST call to <code>/$DB/_compact</code>. Garbage collection is not scheduled automatically, so you have to call it yourself.</paragraph>
         </list-item>
         <list-item>
            <paragraph>Document IDs longer than 180 characters overflow the Couchbase Server maximum key length and cause an HTTP error.</paragraph>
         </list-item>
      </unordered-list>
   </introduction>
</article>
